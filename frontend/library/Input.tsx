import React from "react";
import { Input as InputThemeUi } from "theme-ui";
import { InputProps } from "theme-ui";

const Input = (props: InputProps) => <InputThemeUi {...props} />;

export default Input;
