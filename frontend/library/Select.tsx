import React from "react";
import { Select as SelectThemeUi } from "theme-ui";
import { SelectProps } from "theme-ui";

const Select = (props: SelectProps) => <SelectThemeUi {...props} />;

export default Select;
