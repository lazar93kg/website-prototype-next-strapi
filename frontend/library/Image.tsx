import React from "react";
import { Image as ImageThemeUi } from "theme-ui";
import { ImageProps } from "theme-ui";

const Image = (props: ImageProps) => <ImageThemeUi {...props} />;

export default Image;
